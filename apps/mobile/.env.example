# Written automatically by `bun run cloudflared` (updates EXPO_PUBLIC_API_URL).
# Expo Web (HTTPS): https://riftbounddev.roan.dev — run `bun run dev:mobile` separately
# API on same host: /api/v1/* and /swagger*
# Native simulators can use http://localhost:3000
EXPO_PUBLIC_API_URL=https://riftbounddev.roan.dev
EXPO_DEV_SERVER_ORIGIN=https://riftbounddev.roan.dev
