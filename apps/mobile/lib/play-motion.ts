/**
 * Play scoreboard motion — re-exports the shared app palette.
 * Prefer `@/lib/motion` for new call sites.
 */
export {
  MOTION as PLAY_SPRINGS,
  PULSE_MS as PLAY_PULSE_MS,
  PLAY_PRESS,
} from '@/lib/motion';
